import type {
  EdDSAPrivKey as PrivKey,
  EdDSAPubKey as PubKey,
  EdDSASig as Signature,
  Point,
  VKey,
  WholeProof,
} from "@/crypto/babyzk/deps";

// re-export
export type { PrivKey, PubKey, Signature, Point, VKey, WholeProof };
